//get a two digit number from the user and print the reverse of the number
#include <stdio.h>
int main() {
    int a,u,t,n;
    scanf("%d",&a);
    u=a%10;
    t=a/10;
    n=u*10+t;
    printf("%d",n);
    return 0;
}
