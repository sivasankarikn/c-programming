//get a three digit number from the user and print the sum of the digits
#include <stdio.h>
int main() {
    int a,x,y,z;
    scanf("%d",&a);
    x = a%10;
    y = (a/10)%10;
    z = a/100;
    printf("%d",x+y+z);
    return 0;
}
