//Get a number from the user and check if the number equals 50. If yes, then print "Success", otherwise print "Failure".
#include <stdio.h>
int main() {
    int a,u,t;
    scanf("%d",&a);
    if(a==50)
    printf("success");
    else printf("failure");
    return 0;
}
