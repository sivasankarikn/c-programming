//Get a two-digit number from the user and check if the digits zero and digit one are not identical. If yes, print one, otherwise print zero.
#include <stdio.h>
int main() {
    int a,u,t;
    scanf("%d",&a);
    u=a%10;
    t=a/10;
    printf("%d",u!=t);
    return 0;
}
